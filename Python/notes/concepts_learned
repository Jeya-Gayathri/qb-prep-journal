# What I Learned — My Notes
##Day-10
Heart Rate Moving Averages (Course 1, Module 4)
Question: Implement find_moving_average, get_only_peaks, moving_average_of_peaks for heart rate data analysis
Approach: Built moving average using sliding window (data[i:i+n]) with sum/len; reused is_peak_at from prior assignment via symlink to extract peak values; chained get_only_peaks output directly into find_moving_average for the combined function
Status: 100% — all test cases passed, including edge cases (empty list, single-element windows)
Subsequence Test Cases (Course 1)
Question: Design test cases (check(input, expected)) that would catch specific bugs in a maxSeq (longest contiguous increasing subsequence) implementation
Approach: For each described bug (empty list handling, non-decreasing vs. strictly-increasing confusion, off-by-one iteration errors, wrong return placement, faulty initialization to 0, max-not-updated edge case), constructed inputs where buggy and correct logic would diverge
Status: All 7 questions passed
Duke Course 1 — Python Programming Fundamentals
Status: COMPLETE — Certificate ID T4G2RVW6Y1OY, issued 9 Aug 2026


##Day-08,09 

List Max — Course 2, Module 4 (Diving Deeper with Lists)
**Concept: Lists as references to mutable objects**
Lists in Python are mutable and variables hold *references* to them, not
copies. Assigning `B = A` doesn't create a new list — both names point to
the same object in memory, so changing one changes the other.
This is the same underlying gotcha as the `B = A` vs `B = A.copy()` bug from
the Debugging reading — squaring `B` in that example silently squared `A`
too, because no real copy was ever made. Same pattern will resurface later
in NumPy/Pandas as "views vs copies" — worth remembering as one concept
wearing three names across the course.

**listMax() logic**
- Guard first: if `list` is `None` or empty, return `None` before touching
  anything else.
- Initialize `Max` to the first element rather than 0 or a sentinel — safe
  even for all-negative lists (tested against `list2` above).
- Single pass from index 1, updating `Max` whenever a larger element is found.
- Status: 100%

## Day 4 - Loops, For Loop & f-String
**1. Loop**
- Used to avoid repetition in writing the same code multiple times.
- Two types:
- `for`
- `while`

**2. For Loop**

- Used when we know the number of occurrences/iterations.

- Syntax:
for variable in range(start, stop, step):
# operation to be repeated

**3.f-string**
-directly uses the value of variables and can also evaluate expressions/calculations inside {}
-Ex: print(f"{num} * {i} = {num*i}")

## Day 3 - Python Basics — Concepts Learned So Far

**1. Variables**
- Used to store data values (e.g., `age = 21`)
- Case-sensitive, no spaces, can't start with a number

**2. Data Types**
- `int` (whole numbers), `float` (decimals), `str` (text - to be give data in quote single / double), `bool` (True/False)

**3. Arithmetic Operators**
- `+`, `-`, `*`, `/`, `//` (floor division), `%` (modulus), `**` (exponent)
- Used for basic calculations on numeric variables

**4. Comparison Operators**
- `==` (equal to), `!=` (not equal), `>`, `<`, `>=`, `<=`
- Return `True`/`False` — used to build conditions

**5. If/Else**
- Basic conditional syntax
if condition:
    # code if condition is True
else:
    # code if condition is False

**6. Nested If/Else**
- Checking a second condition only after the first is true
- Syntax
if condition1:
    if condition2:
        # code if both condition1 and condition2 are True
    else:
        # code if condition1 True, condition2 False
else:
    # code if condition1 is False

**7. Elif Chains**
- Multiple conditions checked top to bottom
- Execution stops at the **first True match** — rest are skipped even if also technically true
- Order matters — write from highest/most specific condition down
- Syntax
if condition1:
    # code if condition1 is True
elif condition2:
    # code if condition1 False, condition2 True
elif condition3:
    # code if condition1, condition2 False, condition3 True
else:
    # code if none of the above are True

**8. Range Conditions**
- Using `and`, or Python's chained comparison shortcut (`75 <= marks < 90`)
- Applied to real-world cases: ticket price
**9. Logical Operators**
- and — all conditions must be True
- or — at least one condition must be True
- not — flips True/False
- not in — checks if a value is missing from a list (same as SQL's NOT IN)

**Practice done:**
- Built a **ticket pricing system** using if/elif/else with range conditions (age-based: Free / $100 / $250 / $120)
- Debugged own code — fixed comparison operator errors (`=` vs `==` vs `<=`), invalid operators (`->`, `=>`), case sensitivity (`Print` vs `print`, `Main()` vs `main()`), missing `()` in function definitions

**Key takeaway:**
Understood the concept of **indentation levels** in Python (how if/elif/else blocks and function bodies must be indented to define scope) — but need more hands-on practice to apply this comfortably without errors, especially with nested blocks and functions.

## Day 2 - Indentation & Return

- Indentation shows what code is "inside" what. Like a box inside a box.
- If a line is indented more than the line above it, it belongs to that line.
- `return` stops the function right away. If it's inside a loop, the function 
  stops too early — before the loop finishes.
- `return` should come after the loop is fully done, not inside it.
- I still need more practice deciding indent levels on my own.

## Day 1 - Functions & max()

- `max()` is already built into Python. When I wrote my own `max()`, it worked 
  because Python let me replace it with my version.
- `def` is used to create a function.
- `main()` is not a special Python word. It's just a common name people use 
  to organize the code that runs first.
- `range(size)` starts counting from 0 and stops before size.
